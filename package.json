{
  "name": "cssbattle-previewer-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Run backend for rendering CSSBattle previews.",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=renderPreview",
    "check": "node --check index.js"
  },
  "engines": {
    "node": "24"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.2",
    "puppeteer": "^24.42.0"
  }
}
